import styled from "@emotion/styled";

const MainArticlePage = styled.div`
  position: relative;

  button {
    top: 1.5%;
    left: 3%;
    z-index: 50;
    background: var(--color-primary);
  }
`;

export default MainArticlePage;
