import styled from "@emotion/styled";

const DestinationPageContainer = styled.div`
  padding: 0 1em;
  display: flex;
  flex-direction: column;

  h2,
  h3 {
    text-align: center;
    margin: 0.5rem 0;
  }

  h3 {
    font-weight: normal;
    font-size: 0.9rem;
    margin-bottom: 1.2rem;
  }

  p {
    margin: 0.5rem 0;
    font-size: 0.6rem;
    line-height: 0.8rem;
  }
`;

export default DestinationPageContainer;
