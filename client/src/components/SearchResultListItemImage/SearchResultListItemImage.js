const { default: styled } = require("@emotion/styled");

const SearchResultListItemImage = styled.img`
  width: 25%;
  border-radius: 0.7rem;
  height: 65px;
`;

export default SearchResultListItemImage;
