const { default: styled } = require("@emotion/styled");

const HomeMainSubtitle = styled.h3`
  border-left: 3px solid var(--color-primary);
  margin: 1rem 0.2rem 1rem 0;
  padding-left: 0.5rem;
`;

export default HomeMainSubtitle;
