import styled from "@emotion/styled";

const MenuMainImage = styled.img`
  width: 6rem;
  border: 2.5px solid var(--color-white);
  border-radius: 50%;
  margin-bottom: 1.5rem;
  margin-top: 5rem;
  align-self: center;

  margin-right: 22px;
`;

export default MenuMainImage;
